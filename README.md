# Cache Simulator (C++)

A simple cache emulator written in C++ to understand
cache behavior (hits, misses, direct-mapped cache).

## Build
clang++ -std=gnu++14 -Wall -Wextra cache_simulator.cpp -o cache_simulator

## Run
./cache_simulator

